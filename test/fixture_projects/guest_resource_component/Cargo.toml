[package]
name = "guest_resource_component"
version = "0.1.0"
edition = "2024"

[dependencies]
wit-bindgen = "0.57.1"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
lto = "thin"
opt-level = "z"
codegen-units = 1
strip = true
panic = "abort"
