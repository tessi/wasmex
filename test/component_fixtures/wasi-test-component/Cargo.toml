[package]
name = "wasi-test-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.39.0", features = ["default"] }
getrandom = { version = "0.2", features = ["custom"] }

[profile.release]
opt-level = "s"
lto = true